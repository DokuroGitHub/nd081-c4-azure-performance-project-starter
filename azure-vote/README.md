# cmds

```bash

redis-server.exe

pyenv versions
pyenv global 3.11.0
pip list

python -m venv .venv
.venv/Scripts/activate
python -m pip install --upgrade pip
# rm -r venv

pip install -r requirements.txt
python main.py

```
